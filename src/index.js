import 'babel-polyfill';
import App from './components/App';

new App().render();

// <!-- Step 3b: Fetch server identification (VAPID) key -->
// <!-- Step 3b: Fetch server identification (VAPID) key -->
